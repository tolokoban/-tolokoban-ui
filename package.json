{
    "name": "@tolokoban/ui",
    "version": "0.1.0",
    "private": true,
    "main": "index.ts",
    "type": "module",
    "scripts": {
        "precopy": "rm -rf build",
        "copy": "rsync -av --exclude='**/*.test.*' ./lib/src/* ./build",
        "prebuild": "npm run copy",
        "build": "cd build && npm link"
    }
}