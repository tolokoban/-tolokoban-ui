import { Children } from "../../types";
export interface ViewReadProps {
    className?: string;
    children: Children;
}
export declare function ViewRead({ className, children }: ViewReadProps): import("react/jsx-runtime").JSX.Element;
